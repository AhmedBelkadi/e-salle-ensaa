<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://turntable.proxy.rlwy.net:13477/railway</property>
        <property name="hibernate.connection.username">postgres</property>
        <property name="hibernate.connection.password">yTyljehPwNOanMKFsPkjBDHfwotqHnxO</property>

        <!-- Optional but recommended if Railway requires SSL -->
        <property name="hibernate.connection.sslmode">require</property>

        <!-- JDBC connection pool settings -->
        <property name="hibernate.connection.pool_size">10</property>

        <!-- SQL dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="hibernate.show_sql">false</property>
        <property name="hibernate.format_sql">true</property>
        <property name="hibernate.use_sql_comments">false</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Current Session Context -->
        <property name="hibernate.current_session_context_class">thread</property>

        <!-- Disable the second-level cache -->
        <property name="hibernate.cache.use_second_level_cache">false</property>
        <property name="hibernate.cache.use_query_cache">false</property>

        <!-- ========================================== -->
        <!-- ENTITY CLASS MAPPINGS - CRITICAL!         -->
        <!-- ========================================== -->
        
        <!-- User and Authentication -->
        <mapping class="com.esalle.entity.User"/>
        
        <!-- Room Management -->
        <mapping class="com.esalle.entity.Salle"/>
        <mapping class="com.esalle.entity.Reservation"/>
        <mapping class="com.esalle.entity.Reclamation"/>
        
        <!-- Academic Management -->
        <mapping class="com.esalle.entity.Filiere"/>
        <mapping class="com.esalle.entity.Matiere"/>
        <mapping class="com.esalle.entity.EmploiDuTemps"/>
        
        <!-- Audit -->
        <mapping class="com.esalle.entity.AuditLog"/>

    </session-factory>
</hibernate-configuration>
