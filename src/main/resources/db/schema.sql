-- Database schema for E-Salle ENSAA
-- PostgreSQL database

-- Create database (run this manually if needed)
-- CREATE DATABASE esalle_ensaa;

-- ============================================
-- Add your table definitions here
-- ============================================

-- Example table structure:
/*
CREATE TABLE IF NOT EXISTS table_name (
    id BIGSERIAL PRIMARY KEY,
    field1 VARCHAR(100) NOT NULL,
    field2 VARCHAR(20) NOT NULL UNIQUE,
    field3 INTEGER NOT NULL CHECK (field3 > 0),
    description VARCHAR(500),
    date_creation TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    date_modification TIMESTAMP
);

-- Create indexes for better performance
CREATE INDEX IF NOT EXISTS idx_table_field ON table_name(field);

-- Insert sample data
INSERT INTO table_name (field1, field2, field3) VALUES
('Sample 1', 'S001', 100),
('Sample 2', 'S002', 200);
*/

-- ============================================
-- Add your custom views and functions here
-- ============================================
